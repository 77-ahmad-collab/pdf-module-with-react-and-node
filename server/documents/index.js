module.exports = ({ name1, name2, name3 }) => {
  return `
    <!doctype html>
    <html>
    <body>
    <h2>this is pdf and the naemes atr ${name1}  and name 2 is ${name2}and the name  3is ${name3}</h2>
    </body>
    </html>

    `;
};
